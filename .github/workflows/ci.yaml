name: ci-golang-workfow
on: [push]
jobs:
  check-application:
    runs-on: ububtu-latest
    steps:
      - uses: action/checkout@v2
      - uses: action/setup-go@v2
        with:
          go-version: 1.15
      - run: go test
      - run: go run match.go